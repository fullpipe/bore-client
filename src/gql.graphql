query books {
  books {
    id
    title
    author
    reader
  }
}

query book($id: ID!) {
  book(id: $id) {
    id
    title
    author
    reader

    parts {
      title
      possition
      path
    }
  }
}
